# Secrets — never commit
.env
.env.*

# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
.turbo/

# Test coverage
coverage/

# Local policy config (contains repo-specific tuning — use policy.config.example.json as the template)
policy/config/policy.config.json
